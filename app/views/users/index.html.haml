.background
  .user
    = render 'layouts/user_profile'
  .post
    .box#users-box
      - if @users.any?
        %ul
          = render partial: 'user', collection: @users, as: :user
        = paginate @users
      - else
        = render partial: 'layouts/no_matches'
